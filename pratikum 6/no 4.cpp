#include <stdio.h>

int main() {
    int speed = 2; // Kecepatan dalam meter/detik
    int time = 100; // Waktu dalam detik

    int distance = speed * time;
    printf("Jarak yang ditempuh: %d meter\n", distance);
    return 0;
}
